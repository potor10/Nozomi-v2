// Import Modules
import express from 'express'

// Import Controllers

const router = express.Router()

router.post('/level', () => {return false})
router.post('/ascend', () => {return false})

export default router